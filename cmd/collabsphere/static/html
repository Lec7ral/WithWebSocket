      .typing-indicator {
        flex: 0 0 auto;
        min-height: 0;
        padding: 4px 10px 6px;
        font-size: 11px;
        color: var(--muted);
        display: inline-flex;
        align-items: center;
        gap: 4px;
      }

      .typing-indicator::after {
        content: "● ● ●";
        font-size: 9px;
        letter-spacing: 2px;
        opacity: 0.7;
      }

